name: 'Package source code & dependencies'
description: 'Package source code & dependencies for SAST & SCA scans'
author: 'Fortify'
runs:
  using: composite
  steps:
    - uses: fortify/github-action/internal/setup-java-for-sc-client@714fe84b03a813fe8124e5ef4ce22da010ab1839
    - uses: fortify/github-action/setup@714fe84b03a813fe8124e5ef4ce22da010ab1839
      with:
        export-path: false
        sc-client: action-default
    - uses: fortify/github-action/internal/run-script@714fe84b03a813fe8124e5ef4ce22da010ab1839
      with:
        script: package.sh
    - name: Store ScanCentral package
      if: env.DO_PACKAGE_DEBUG == 'true'
      uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f
      with:
        name: ${{ github.job }}-${{ strategy.job-index }}-scancentral-package
        path: package.zip
    - name: Store ScanCentral logs
      if: env.DO_PACKAGE_DEBUG == 'true'
      uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f
      with:
        name: ${{ github.job }}-${{ strategy.job-index }}-scancentral-logs
        path: |
          ~/.fortify/scancentral-*/log/*.log
          ~/AppData/Local/Fortify/scancentral-*/log/*.log
        include-hidden-files: true
        
branding:
  icon: 'shield'
  color: 'blue'

