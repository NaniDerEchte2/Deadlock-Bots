name: "🔒 Deep Security Scan (Optimized)"

on:
  push:
    branches: [ "main", "master", "develop" ]
  pull_request:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

permissions:
  contents: read
jobs:
  # ============================================================================
  # CHECK WHAT'S AVAILABLE
  # ============================================================================
  detect-languages:
    name: 🔍 Detect Project Structure
    runs-on: ubuntu-latest
    outputs:
      has_python: ${{ steps.check.outputs.has_python }}
      has_javascript: ${{ steps.check.outputs.has_javascript }}
      has_requirements: ${{ steps.check.outputs.has_requirements }}
      has_package_json: ${{ steps.check.outputs.has_package_json }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      
      - name: Check Project Structure
        id: check
        run: |
          # Check for Python files
          if find . -name "*.py" -type f | grep -q .; then
            echo "has_python=true" >> $GITHUB_OUTPUT
            echo "✅ Python files detected"
          else
            echo "has_python=false" >> $GITHUB_OUTPUT
            echo "ℹ️ No Python files found"
          fi
          
          # Check for JS/TS files
          if find . -name "*.js" -o -name "*.ts" -o -name "*.jsx" -o -name "*.tsx" | grep -q .; then
            echo "has_javascript=true" >> $GITHUB_OUTPUT
            echo "✅ JavaScript/TypeScript files detected"
          else
            echo "has_javascript=false" >> $GITHUB_OUTPUT
            echo "ℹ️ No JavaScript files found"
          fi
          
          # Check for Python dependency manifests recursively
          if find . -type f -name "requirements*.txt" \
            -not -path "*/.venv/*" \
            -not -path "*/venv/*" \
            -not -path "*/.git/*" | grep -q .; then
            echo "has_requirements=true" >> $GITHUB_OUTPUT
            echo "✅ requirements*.txt files found"
          else
            echo "has_requirements=false" >> $GITHUB_OUTPUT
            echo "ℹ️ No requirements*.txt files found"
          fi
          
          # Check for package.json recursively
          if find . -type f -name "package.json" \
            -not -path "*/node_modules/*" \
            -not -path "*/.git/*" | grep -q .; then
            echo "has_package_json=true" >> $GITHUB_OUTPUT
            echo "✅ package.json files found"
          else
            echo "has_package_json=false" >> $GITHUB_OUTPUT
            echo "ℹ️ No package.json files found"
          fi

  # ============================================================================
  # PYTHON SECURITY - ONLY IF PYTHON EXISTS
  # ============================================================================
  python-security:
    name: 🐍 Python Security Suite
    needs: detect-languages
    if: needs.detect-languages.outputs.has_python == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: '3.12'

      # Bandit - Security Issues
      - name: Bandit Security Scan
        run: |
          echo "Running Bandit security scan..."
          python -m pip install bandit==1.8.3
          bandit -r . -f json -o bandit-report.json -ll
          echo "✅ Bandit scan completed"
          bandit -r . -ll
      # Safety - Known Vulnerabilities (recursive requirements scan)
      - name: Safety Vulnerability Check
        if: needs.detect-languages.outputs.has_requirements == 'true'
        run: |
          echo "Running Safety vulnerability check (recursive requirements scan)..."
          python -m pip install safety==3.7.0
          mkdir -p safety-reports
          found=0
          audit_fail=0
          while IFS= read -r req_file; do
            found=1
            rel_path="${req_file#./}"
            safe_name="$(echo "$rel_path" | sed 's#[^A-Za-z0-9._-]#_#g')"
            echo "Safety scan for $req_file"
            if ! safety check -r "$req_file" --json > "safety-reports/${safe_name}.json"; then
              audit_fail=1
            fi
          done < <(find . -type f -name "requirements*.txt" \
            -not -path "*/.venv/*" \
            -not -path "*/venv/*" \
            -not -path "*/.git/*" | sort)

          if [ "$found" -eq 0 ]; then
            echo "ℹ️ No requirements*.txt files found for Safety scan"
          else
            echo "✅ Safety recursive scan completed"
          fi

          if [ "$audit_fail" -ne 0 ]; then
            echo "❌ Safety found vulnerable dependencies (see artifacts)."
            exit 1
          fi
      # Pip-Audit (recursive requirements scan)
      - name: Pip-Audit Deep Scan
        if: needs.detect-languages.outputs.has_requirements == 'true'
        run: |
          echo "Running pip-audit (recursive requirements scan)..."
          python -m pip install pip-audit==2.8.2
          mkdir -p pip-audit-reports
          found=0
          audit_fail=0
          while IFS= read -r req_file; do
            found=1
            rel_path="${req_file#./}"
            safe_name="$(echo "$rel_path" | sed 's#[^A-Za-z0-9._-]#_#g')"
            echo "pip-audit for $req_file"
            if ! pip-audit -r "$req_file" --format json --output "pip-audit-reports/${safe_name}.json"; then
              audit_fail=1
            fi
          done < <(find . -type f -name "requirements*.txt" \
            -not -path "*/.venv/*" \
            -not -path "*/venv/*" \
            -not -path "*/.git/*" | sort)

          if [ "$found" -eq 0 ]; then
            echo "ℹ️ No requirements*.txt files found for pip-audit"
          else
            echo "✅ Pip-audit recursive scan completed"
          fi

          if [ "$audit_fail" -ne 0 ]; then
            echo "❌ pip-audit found vulnerable dependencies (see artifacts)."
            exit 1
          fi
      # Radon - Complexity Analysis
      - name: Complexity Analysis
        run: |
          echo "Running complexity analysis..."
          python -m pip install radon==6.0.1
          radon cc . -a -nb --total-average > radon-complexity.txt || true
          radon mi . -nb > radon-maintainability.txt || true
          echo "✅ Complexity analysis completed"
      # Vulture - Dead Code Detection
      - name: Find Dead Code
        run: |
          echo "Looking for dead code..."
          python -m pip install vulture==2.14
          vulture . --min-confidence 80 > vulture-report.txt || true
          if [ -f vulture-report.txt ]; then
            lines=$(wc -l < vulture-report.txt)
            echo "Found $lines potential dead code items"
          fi
      # Dodgy - Suspicious code patterns
      - name: Dodgy Suspicious Pattern Check
        run: |
          echo "Running dodgy..."
          python -m pip install dodgy==0.2.1
          dodgy > dodgy-report.txt
      - name: Upload Python Security Reports
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        if: always()
        with:
          name: python-security-reports
          path: |
            bandit-report.json
            safety-reports/*.json
            pip-audit-reports/*.json
            radon-*.txt
            vulture-report.txt
            dodgy-report.txt
          if-no-files-found: ignore

  # ============================================================================
  # JAVASCRIPT SECURITY - ONLY IF JS/TS EXISTS
  # ============================================================================
  javascript-security:
    name: 📦 JavaScript/TypeScript Security
    needs: detect-languages
    if: needs.detect-languages.outputs.has_javascript == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Node.js
        uses: actions/setup-node@6044e13b5dc448c55e2357c09f80417699197238 # v6.2.0
        with:
          node-version: '20'

      - name: Property-Based Fuzzing (fast-check)
        if: hashFiles('cogs/steam/steam_presence/fuzz/*.js') != ''
        run: |
          npm ci --prefix cogs/steam/steam_presence --include=dev --ignore-scripts
          npm --prefix cogs/steam/steam_presence run fuzz:protocol --if-present
      # NPM Audit - recursive lockfile scan
      - name: NPM Audit
        if: hashFiles('**/package-lock.json', '**/npm-shrinkwrap.json') != ''
        run: |
          echo "Running NPM audit (recursive lockfile scan)..."
          mkdir -p npm-audit-reports
          found=0
          audit_fail=0
          while IFS= read -r lock_file; do
            found=1
            dir_path="$(dirname "$lock_file")"
            rel_dir="${dir_path#./}"
            safe_name="$(echo "${rel_dir:-root}" | sed 's#[^A-Za-z0-9._-]#_#g')"
            echo "NPM audit for $dir_path"
            if ! (
              cd "$dir_path"
              npm audit --json > "$GITHUB_WORKSPACE/npm-audit-reports/${safe_name}.json"
            ); then
              audit_fail=1
            fi
          done < <(find . -type f \( -name "package-lock.json" -o -name "npm-shrinkwrap.json" \) \
            -not -path "*/node_modules/*" \
            -not -path "*/.git/*" | sort)

          if [ "$found" -eq 0 ]; then
            echo "ℹ️ No npm lockfiles found for recursive audit"
          else
            echo "✅ NPM recursive audit completed"
          fi

          if [ "$audit_fail" -ne 0 ]; then
            echo "❌ npm audit found vulnerable dependencies (see artifacts)."
            exit 1
          fi
      # RetireJS - Check for vulnerable JS libraries
      - name: RetireJS Scan
        run: |
          echo "Running RetireJS scan..."
          npx --yes retire@5.4.2 --path . --outputformat json --outputpath retirejs-report.json
          if [ -f retirejs-report.json ]; then
            echo "✅ RetireJS scan completed"
          fi
      # ESLint Security Plugin
      - name: ESLint Security Scan
        if: hashFiles('**/package.json') != ''
        run: |
          npx --yes -p eslint@10.0.0 -p eslint-plugin-security@3.0.1 -p eslint-plugin-no-secrets@2.2.2 eslint . --ext .js,.ts,.jsx,.tsx --format json --output-file eslint-security.json
      # Snyk Scan (optional, requires repository secret SNYK_TOKEN)
      - name: Snyk Security Scan
        if: hashFiles('**/package-lock.json', '**/npm-shrinkwrap.json') != ''
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        run: |
          if [ -z "${SNYK_TOKEN:-}" ]; then
            echo "ℹ️ SNYK_TOKEN not configured; skipping Snyk scan."
            exit 0
          fi
          npx --yes snyk@1.1302.1 auth "$SNYK_TOKEN"
          npx --yes snyk@1.1302.1 test --all-projects --severity-threshold=low --json-file-output=snyk-report.json
      - name: Upload JS Security Reports
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        if: always()
        with:
          name: javascript-security-reports
          path: |
            npm-audit-reports/*.json
            retirejs-report.json
            eslint-security.json
            snyk-report.json
          if-no-files-found: ignore

  # ============================================================================
  # SEMGREP SAST - KEEPS GITHUB CODE SCANNING ALERTS IN SYNC
  # ============================================================================
  semgrep-sast:
    name: 🔍 Semgrep OSS SAST
    needs: detect-languages
    if: needs.detect-languages.outputs.has_python == 'true' || needs.detect-languages.outputs.has_javascript == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    container:
      image: semgrep/semgrep:1.151.0
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run Semgrep (JSON)
        run: |
          semgrep scan \
            --config auto \
            --error \
            --json \
            --output semgrep.json \
            --exclude .venv \
            --exclude venv \
            --exclude node_modules \
            --exclude dist \
            --exclude build \
            .
      - name: Run Semgrep (SARIF for Code Scanning)
        run: |
          semgrep scan \
            --config auto \
            --error \
            --sarif \
            --output semgrep.sarif \
            --exclude .venv \
            --exclude venv \
            --exclude node_modules \
            --exclude dist \
            --exclude build \
            .
      - name: Run Semgrep (Custom Auth Header Guardrails)
        if: hashFiles('.semgrep/rules/*.yml', '.semgrep/rules/*.yaml') != ''
        run: |
          semgrep scan \
            --config .semgrep/rules \
            --error \
            --sarif \
            --output semgrep-custom.sarif \
            --exclude .venv \
            --exclude venv \
            --exclude node_modules \
            --exclude dist \
            --exclude build \
            .
      - name: Upload Semgrep Results to Security Tab
        uses: github/codeql-action/upload-sarif@9e907b5e64f6b83e7804b09294d44122997950d6 # v4
        if: always() && hashFiles('semgrep.sarif') != ''
        with:
          sarif_file: semgrep.sarif
          # Keep the historical category stable so existing Semgrep alerts can auto-close.
          category: semgrep
      - name: Upload Custom Semgrep Results to Security Tab
        uses: github/codeql-action/upload-sarif@9e907b5e64f6b83e7804b09294d44122997950d6 # v4
        if: always() && hashFiles('semgrep-custom.sarif') != ''
        with:
          sarif_file: semgrep-custom.sarif
          category: semgrep-custom-auth
      - name: Upload Semgrep Artifact
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        if: always()
        with:
          name: semgrep-reports
          path: |
            semgrep.json
            semgrep.sarif
            semgrep-custom.sarif
          if-no-files-found: ignore

  # ============================================================================
  # TRIVY - FILESYSTEM SCAN (ALWAYS RUNS)
  # ============================================================================
  trivy-scan:
    name: 🛡️ Trivy Security Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      # Filesystem Scan
      - name: Trivy Filesystem Scan
        uses: aquasecurity/trivy-action@c1824fd6edce30d7ab345a9989de00bbd46ef284 # master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'sarif'
          output: 'trivy-fs.sarif'
          severity: 'HIGH,CRITICAL'
          exit-code: 1
      - name: Upload Trivy Results to Security Tab
        uses: github/codeql-action/upload-sarif@9e907b5e64f6b83e7804b09294d44122997950d6 # v4
        if: always() && hashFiles('trivy-fs.sarif') != ''
        with:
          sarif_file: 'trivy-fs.sarif'
          category: 'trivy-filesystem'
      - name: Upload Trivy Artifact
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        if: always()
        with:
          name: trivy-reports
          path: trivy-fs.sarif
          if-no-files-found: ignore

  # ============================================================================
  # DEPENDENCY ANALYSIS (ALWAYS RUNS)
  # ============================================================================
  dependency-analysis:
    name: 📊 Dependency Analysis
    needs: detect-languages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up Python
        if: needs.detect-languages.outputs.has_requirements == 'true'
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: '3.12'

      - name: Setup Node.js
        if: needs.detect-languages.outputs.has_package_json == 'true'
        uses: actions/setup-node@6044e13b5dc448c55e2357c09f80417699197238 # v6.2.0
        with:
          node-version: '20'

      # Python dependency manifests (no dynamic package install in CI)
      - name: Python Dependency Manifest Snapshot
        if: needs.detect-languages.outputs.has_requirements == 'true'
        run: |
          mkdir -p dependency-analysis/python-deps
          found=0
          while IFS= read -r req_file; do
            found=1
            rel_path="${req_file#./}"
            safe_name="$(echo "$rel_path" | sed 's#[^A-Za-z0-9._-]#_#g')"
            cp "$req_file" "dependency-analysis/python-deps/${safe_name}.requirements.txt"
            awk '
              /^[[:space:]]*#/ || /^[[:space:]]*$/ { next }
              { total += 1 }
              /==/ { pinned += 1 }
              END {
                printf("total_entries=%d\n", total)
                printf("pinned_entries=%d\n", pinned)
                if (total > 0) {
                  printf("pinning_ratio=%.2f\n", pinned / total)
                } else {
                  printf("pinning_ratio=1.00\n")
                }
              }
            ' "$req_file" > "dependency-analysis/python-deps/${safe_name}.pinning.txt"
          done < <(find . -type f -name "requirements*.txt" \
            -not -path "*/.venv/*" \
            -not -path "*/venv/*" \
            -not -path "*/.git/*" | sort)
          if [ "$found" -eq 0 ]; then
            echo "ℹ️ No requirements*.txt files found for dependency manifest snapshot"
          fi
      - name: Python Dependency Vulnerability Report
        if: needs.detect-languages.outputs.has_requirements == 'true'
        run: |
          echo "Running pip-audit report..."
          python -m pip install pip-audit==2.8.2
          mkdir -p dependency-analysis/python-vulns
          found=0
          audit_fail=0
          while IFS= read -r req_file; do
            found=1
            rel_path="${req_file#./}"
            safe_name="$(echo "$rel_path" | sed 's#[^A-Za-z0-9._-]#_#g')"
            if ! pip-audit -r "$req_file" --format json --output "dependency-analysis/python-vulns/${safe_name}.pip-audit.json"; then
              audit_fail=1
            fi
          done < <(find . -type f -name "requirements*.txt" \
            -not -path "*/.venv/*" \
            -not -path "*/venv/*" \
            -not -path "*/.git/*" | sort)
          if [ "$found" -eq 0 ]; then
            echo "ℹ️ No requirements*.txt files found for Python vulnerability reporting"
          fi
          if [ "$audit_fail" -ne 0 ]; then
            echo "❌ Python dependency vulnerability report detected issues."
            exit 1
          fi
      # NPM License Check
      - name: NPM License Scanner
        if: needs.detect-languages.outputs.has_package_json == 'true' && hashFiles('**/package-lock.json', '**/npm-shrinkwrap.json') != ''
        run: |
          mkdir -p dependency-analysis/npm-licenses
          found=0
          while IFS= read -r lock_file; do
            found=1
            dir_path="$(dirname "$lock_file")"
            rel_dir="${dir_path#./}"
            safe_name="$(echo "${rel_dir:-root}" | sed 's#[^A-Za-z0-9._-]#_#g')"
            (
              cd "$dir_path"
              npx --yes license-checker@25.0.1 --json > "$GITHUB_WORKSPACE/dependency-analysis/npm-licenses/${safe_name}.json" || true
              npx --yes license-checker@25.0.1 --summary > "$GITHUB_WORKSPACE/dependency-analysis/npm-licenses/${safe_name}.txt" || true
            )
          done < <(find . -type f \( -name "package-lock.json" -o -name "npm-shrinkwrap.json" \) \
            -not -path "*/node_modules/*" \
            -not -path "*/.git/*" | sort)
          if [ "$found" -eq 0 ]; then
            echo "ℹ️ No npm lockfiles found for license scan"
          fi
      - name: Upload Dependency Reports
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        if: always()
        with:
          name: dependency-analysis
          path: |
            dependency-analysis/
          if-no-files-found: ignore

  # ============================================================================
  # OSSF SCORECARD (RESTORED)
  # ============================================================================
  security-scorecard:
    name: 🏆 OSSF Security Scorecard
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      id-token: write
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Run Scorecard
        uses: ossf/scorecard-action@4eaacf0543bb3f2c246792bd56e8cdeffafb205a # v2.4.3
        with:
          results_file: scorecard-results.sarif
          results_format: sarif
          publish_results: true
      - name: Upload Scorecard SARIF
        uses: github/codeql-action/upload-sarif@9e907b5e64f6b83e7804b09294d44122997950d6 # v4
        if: always() && hashFiles('scorecard-results.sarif') != ''
        with:
          sarif_file: scorecard-results.sarif
          category: scorecard
      - name: Upload Scorecard Artifact
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        if: always()
        with:
          name: scorecard-report
          path: scorecard-results.sarif
          if-no-files-found: ignore

  # ============================================================================
  # SUMMARY
  # ============================================================================
  generate-summary:
    name: 📋 Generate Summary
    needs: [detect-languages, python-security, javascript-security, semgrep-sast, trivy-scan, dependency-analysis, security-scorecard]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Create Summary
        run: |
          echo "# 🔒 Security Analysis Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Project Detection:" >> $GITHUB_STEP_SUMMARY
          echo "- Python: ${{ needs.detect-languages.outputs.has_python == 'true' && '✅ Yes' || '❌ No' }}" >> $GITHUB_STEP_SUMMARY
          echo "- JavaScript: ${{ needs.detect-languages.outputs.has_javascript == 'true' && '✅ Yes' || '❌ No' }}" >> $GITHUB_STEP_SUMMARY
          echo "- requirements*.txt: ${{ needs.detect-languages.outputs.has_requirements == 'true' && '✅ Found' || '❌ Not found' }}" >> $GITHUB_STEP_SUMMARY
          echo "- package.json: ${{ needs.detect-languages.outputs.has_package_json == 'true' && '✅ Found' || '❌ Not found' }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Scans Executed:" >> $GITHUB_STEP_SUMMARY
          echo "- Python Security: ${{ needs.python-security.result == 'success' && '✅' || needs.python-security.result == 'skipped' && '⏭️ Skipped' || '❌' }}" >> $GITHUB_STEP_SUMMARY
          echo "- JavaScript Security: ${{ needs.javascript-security.result == 'success' && '✅' || needs.javascript-security.result == 'skipped' && '⏭️ Skipped' || '❌' }}" >> $GITHUB_STEP_SUMMARY
          echo "- Semgrep SAST: ${{ needs.semgrep-sast.result == 'success' && '✅' || needs.semgrep-sast.result == 'skipped' && '⏭️ Skipped' || '❌' }}" >> $GITHUB_STEP_SUMMARY
          echo "- Trivy Scan: ${{ needs.trivy-scan.result == 'success' && '✅' || '❌' }}" >> $GITHUB_STEP_SUMMARY
          echo "- Dependency Analysis: ${{ needs.dependency-analysis.result == 'success' && '✅' || '❌' }}" >> $GITHUB_STEP_SUMMARY
          echo "- OSSF Scorecard: ${{ needs.security-scorecard.result == 'success' && '✅' || '❌' }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📥 **Check artifacts for detailed reports**" >> $GITHUB_STEP_SUMMARY
