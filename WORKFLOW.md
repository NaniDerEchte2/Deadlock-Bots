2026-04-13
- Aufgabe aufgenommen: Fixes für `cogs/player_finder.py` und `cogs/lfg.py` analysiert.
- Geplante Änderungen: LFG-Doppeltrigger entfernen, Unverifiziert-Rollen unterstützen, Offline-Filter für Kandidaten ergänzen.
- Umsetzung abgeschlossen: gezielte Änderungen in beiden Cogs eingebaut, nur an den angeforderten Stellen.
- Verifikation läuft: Syntaxprüfung mit `py_compile`, zusätzlich `ruff check` falls verfügbar.
